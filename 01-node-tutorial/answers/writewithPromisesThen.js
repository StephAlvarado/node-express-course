//const { writeFile, readFile} = require('fs').promises


//const start= async () => {
  //  try{

//start.then(() => {  
 //   return writeFile(
   //     './content/temp.txt',`
//Here is the result : ${first}, ${second}`,
  //          {flag: 'a'}
  //      )  //write line 2.  
    // Return the promise so you can chain the .then statements  
 //})  
 //.then 
  //  return readFile ('./content/first.txt' , 'utf8')
//.then

//return readfile (const first = await readFile('./content/second.txt' , 'utf8'))// write the third line, and follow that with two more .then blocks,  
 // one to call readFile to read it back out, and one to log the data to the screen.  
 //...  
// .catch((error) => {  
 //    console.log("An error occurred: ", error)  
 //}) }}
